{
  "name": "react-states",
  "private": true,
  "version": "0.0.1",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "start": "lerna run start --stream",
    "exec:drilldown": "lerna exec --scope '@demo/drilldown'",
    "start:drilldown": "yarn exec:drilldown -- yarn run start --stream",
    "start:hooks": "lerna exec --scope '@demo/hooks' -- yarn run start --stream",
    "exec:redux": "lerna exec --scope '@demo/redux'",
    "start:redux": "yarn exec:redux -- yarn run start --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "@emotion/react": "^11.7.1",
    "@emotion/styled": "^11.6.0",
    "@mui/icons-material": "^5.4.1",
    "@mui/material": "^5.4.1",
    "@mui/styles": "^5.4.1"
  }
}
